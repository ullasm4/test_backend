services:
  backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DATABASE_URL=${DATABASE_URL}
      - DB_SSL=${DB_SSL:-true}
    env_file:
      - .env
    restart: unless-stopped
